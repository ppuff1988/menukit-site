{
  "name": "menukit-landing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "check": "make check",
    "dev": "wrangler pages dev public --live-reload",
    "pages:create": "wrangler pages project create menukit --production-branch main",
    "deploy": "wrangler pages deploy public --project-name menukit"
  },
  "devDependencies": {
    "wrangler": "4.100.0"
  }
}
