{
  "name": "menukit-landing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "wrangler pages dev public --live-reload",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "4.100.0"
  }
}
